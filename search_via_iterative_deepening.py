import math, re, sys # Import external modules.
import component_ingestInput as ingest# Import internal modules. [IO handler] 
from component_stateRepresentation import * # [state handler]
def dfs(root):
    return DFS(1, root, searchInd, previousNode, Ds, S, children)
# START =========================================
ingest.ingest_input(sys.argv)
tasks = ingest.tasks_T
processors = ingest.processors_P
sort_heuristically(tasks, processors, ingest.timeLimit_D, ingest.target_S)
initialize(tasks, processors);
print("list:",generateList)
generateChildren();
# OPERATE =======================================
traversed=dfs(root)
printTree(traversed)
printFullTree()
